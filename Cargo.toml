[package]
name = "terminal-assistant"
version = "0.1.0"
edition = "2021"
authors = ["Prithibi <prithibi@cachyos>"]
description = "AI-powered terminal command assistant and autocomplete daemon for Linux/CachyOS"
license = "MIT"

[dependencies]
tokio = { version = "1.43", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
clap = { version = "4.5", features = ["derive"] }
reqwest = { version = "0.12", features = ["json"] }
regex = "1.11"
dirs = "6.0"
async-trait = "0.1"
chrono = { version = "0.4", features = ["serde"] }

[[bin]]
name = "terminal-assistantd"
path = "src/bin/daemon.rs"

[[bin]]
name = "terminal-assistant"
path = "src/bin/cli.rs"

[lib]
name = "terminal_assistant"
path = "src/lib.rs"
